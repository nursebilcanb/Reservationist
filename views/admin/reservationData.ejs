<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>
            <% if (typeof(pageTitle) != "undefined") { %>
            <h1 class="mt-4"><%= pageTitle %></h1>
        <% } %></title>
        <!-- Favicon-->
        <link rel="icon" type="image/x-icon" href="/static/admin/assets/favicon.ico" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="/static/admin/css/styles.css" rel="stylesheet" />
    </head>
    <body>
        <div class="d-flex" id="wrapper">
            <!-- Sidebar-->
            <%- include('_sidebar.ejs') %>
            <!-- Page content wrapper-->
            <div id="page-content-wrapper">
                <!-- Top navigation-->
                <%- include('_navbar.ejs') %>
                <!-- Page content-->
                <div class="container-fluid">
                    <% if (typeof(contentTitle) != "undefined") { %>
                        <h1 class="mt-4"> <%= contentTitle %></h1>
                    <% } %>
                <p>The starting state of the menu will appear collapsed on smaller screens, and will appear non-collapsed on larger screens. When toggled using the button below, the menu will change.</p>
                
                <table class="table table-bordered">
                    <thead>
                      <tr>
                        <th scope="col">#</th>
                        <th scope="col">Table No</th>
                        <th scope="col">Reservation Date</th>
                        <th scope="col">Reservation Time</th>
                        <th scope="col">Full Name</th>
                        <th scope="col">People</th>
                        <th scope="col">Email</th>
                        <th scope="col">Tel No</th>
                        <th scope="col">Special Request</th>
                        <th scope="col" class="text-end ">Processing</th>
                      </tr>
                    </thead>
                    <tbody>
                    <% if (Array.isArray(data)) { %>
                        <% data.forEach(element => { %>
                            <tr>
                                <th scope="row"><%= element.id %></th>
                                <td><%= element.tableNo %></td>
                                <td><%= element.date %></td>
                                <td><%= element.time %></td>
                                <td><%= element.fullName %></td>
                                <td><%= element.people %></td>
                                <td><%= element.email %></td>
                                <td><%= element.telNo %></td>
                                <td><%= element.specialRequest %></td>
                                <td class="text-end text-nowrap">
                                    <a href="/admin/panel/reservationData/edit/<%= element.id %>" class="btn btn-primary">Edit</a>
                                    <a href="/admin/panel/reservationData/delete/<%= element.id %>" class="delete-btn btn btn-danger">Delete</a>
                                </td>
                              </tr>
                        <% }) %>
                         <% } %>     
                    </tbody>
                  </table>
                
                </div>
            </div>
        </div>
        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Core theme JS-->
        <script src="/static/admin/js/scripts.js"></script>
    </body>
</html>


